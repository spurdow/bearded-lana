<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bl.app.com.be_live">

    <application android:allowBackup="true" android:name="com.app.belive.BLApplication"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.AppCompat">

        <activity android:name="com.app.belive.Splash" android:configChanges="orientation|keyboardHidden">

        </activity>

        <activity android:name="com.app.belive.Registration" android:configChanges="orientation|keyboardHidden">

        </activity>

        <activity android:name="com.app.belive.ActionBarTester" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.facebook.LoginActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id_test" />

    </application>

</manifest>
